# Copy to .env and fill values for local development (do NOT commit .env)
PORT=8000
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=changeme
DB_NAME=MOU-Records
ALLOW_DEV_ENDPOINTS=true
APP_BASE=http://localhost:8000
DB_HOST=localhost
DB_USER=postgres
DB_NAME=MOU-Records
DB_PASSWORD=pgAdmin1234
DB_PORT=5432

# Optional: change PORT used by Fresh (default 8000)
PORT=8000
